# Bootstrap checkpoint

The bootstrap is complete only after the branch's exact-head cross-platform CI is terminal-green and the PR is integrated. Until then these capabilities are provisional branch work. Do not create a second bootstrap implementation; repair this branch/PR or its explicit successor.
