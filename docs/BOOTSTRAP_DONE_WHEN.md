# Bootstrap done when

The bootstrap is DONE only after: branch has one reviewable PR; exact-head Windows+Ubuntu core CI is terminal green; causal replay, Decimal ledger and copyability regressions pass; no unresolved blocker invalidates those claims; branch is integrated to main. Documentation count is irrelevant to completion.
