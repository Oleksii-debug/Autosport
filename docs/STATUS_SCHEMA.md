# Status reporting schema

Every meaningful development report should include four compact facts: `READY_NOW` (user-facing capabilities actually usable/proven), `NOT_READY` (nearest important missing capabilities), `EVIDENCE` (PR/head/tests/artifact), and `WHOLE_PRODUCT_PROGRESS` (conservative percentage from `PROGRESS.md`). Optional technical detail follows these facts rather than replacing them.
